function greeter(person) {
    return "Hello, " + person;
}

var user = "Jane User";

/*
 function greeter(person: string) {
 return "Hello, " + person;
 }
 var user = "Jane User";
 var user = [0, 1, 2];
 */

console.log(greeter(user));
